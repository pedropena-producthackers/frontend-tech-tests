'use client';
// import { useActionState } from 'react';
// import { useFormStatus } from 'react-dom';
// import { subscribeAction } from './actions';

// TODO:
//   - useActionState(subscribeAction, { success: false, errors: {} })
//   - Inputs: name, email con mensajes de error por campo
//   - <SubmitButton /> que use useFormStatus → "Enviando..." + disabled
//   - Mensaje de éxito cuando state.success

export function NewsletterForm() {
  return (
    <form>
      {/* Tu código aquí */}
    </form>
  );
}
