const API_URL = 'https://jsonplaceholder.typicode.com/posts';

// TODO: implementa:
//   fetchNews()              → primeros 10 artículos
//   renderSkeleton()         → skeleton/spinner mientras carga
//   renderArticles(articles) → pinta la lista
//   renderError(message)     → error + botón "Reintentar"
//   setupSearch(articles)    → filtrado en cliente con debounce

document.addEventListener('DOMContentLoaded', () => {
  // Tu código aquí
});
