'use client';
// import { useActionState } from 'react';
// import { useFormStatus } from 'react-dom';
// import { subscribeAction } from '@/app/newsletter/actions';

// TODO:
//   - useActionState(subscribeAction, { success: false, errors: {} })
//   - Inputs name y email con mensajes de error por campo (aria-invalid, aria-describedby).
//   - <SubmitButton /> con useFormStatus → "Enviando..." + disabled.
//   - Mensaje de confirmación cuando state.success.

export function NewsletterForm() {
  return (
    <form>
      {/* Tu código aquí */}
    </form>
  );
}
